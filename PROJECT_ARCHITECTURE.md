# Zotero Superior Intelligence (SI) 插件工程架构（Project Architecture）

本工程负责构建支持 Zotero 7 / 10 运行时的现代科研 AI 文献助手插件，深度集成 MinerU 高精度文档解析能力。
输入为研究者选中的文献与 PDF 附件、MinerU 云端解析 API 契约及用户科研大模型配置。
输出为可直接安装至 Zotero 客户端的现代化扩展安装包（`.xpi`）、全链路测试套件与规范化开源代码库。

---

## 关键目录与文件清单

| 文件 / 目录名称 | 地位 | 功能描述 | 输入数据 | 输出数据 |
| :--- | :--- | :--- | :--- | :--- |
| `README.md` | 开源门户主页 | GitHub 项目主页，提供中英双语介绍、特性矩阵、安装与配置指南 | 项目全景信息 | 开发者与用户第一入口 |
| `LICENSE` | 开源许可协议 | AGPL-3.0 开源协议文本，规范开源衍生与权利声明 | 开源法律规范 | 法律使用许可 |
| `package.json` | 工程元数据配置 | 声明包名、版本号（v1.0.0）与常用 npm 脚本快捷指令 | 项目元数据 | npm 运行配置与元数据 |
| `update.json` | 自动更新元数据清单 | 声明供 Zotero 客户端自动轮询检测最新版本与 XPI 下载链接 | 版本号与 Release 下载地址 | Zotero 自动更新检测数据源 |
| `.gitignore` | 版本控制忽略规则 | 排除中间编译安装包、第三方反编译源码、Python/Node 缓存与敏感凭证 | 文件路径规则 | 纯净的 Git 代码树 |
| `.github/` | CI/CD 持续集成 | 包含 GitHub Actions 自动化工作流，每次 Push/PR 自动测试与打包 | 仓库代码变动 | 自动化测试报告与 XPI 产物 |
| `PROJECT_ARCHITECTURE.md` | 根架构文档 | 阐述整个插件工程的科学职责、模块分布与数据流向 | 工程目录与文件结构 | 架构全景索引 |
| `EXPERIMENT_LOG.md` | 实验开发日志 | 记录插件重构、解包分析、接口接入、版本构建与 GitHub 整理全过程 | 研发活动与测试记录 | 可追溯的研发日志 |
| `build_xpi.py` | 核心构建脚本 | 校验源码清单、自动打包必要资产并输出标准 Zotero `.xpi` 安装包 | `plugin_src/` 源码 | `dist/zotero-superior-intelligence-*.xpi` |
| `plugin_src/` | 插件核心源码 | 包含插件清单、生命周期、MinerU 客户端、大模型通信与 UI 逻辑 | 用户代码与静态资源 | 待打包的插件源码系统 |
| `tests/` | 自动化测试套件 | 包含 10 个离线单元与集成测试脚本，覆盖渲染、高亮、全库检索等 | 虚拟环境与 Mock 数据 | 测试断言与回归验证报告 |
| `docs/` | 技术规范与文档 | 沉淀 MinerU API 技术契约、算法设计方案与无环境浏览器 UI 预览 | 官方接口与设计规范 | 规范文档与界面预览截图 |
| `dist/` | 分发打包目录 | 存放构建产出的最终 `.xpi` 安装包与发布目录架构文档 | `build_xpi.py` 执行产物 | Zotero 插件安装包 |
| `template/` | 模板解构目录 | 存储与解构早期参考插件，保留开源资产供架构设计参考 | 原始参考模板文件 | 解构后的参考资料与 URL 索引 |

---

## 系统数据流架构图

```
 ┌────────────────────────────────────────────────────────┐
 │                      Zotero 10                         │
 │  ┌──────────────────────┐    ┌──────────────────────┐  │
 │  │ 文献条目与 PDF 附件   │    │  原生 PDF 阅读器界面  │  │
 │  └──────────┬───────────┘    └──────────┬───────────┘  │
 └─────────────┼───────────────────────────┼──────────────┘
               ▼                           ▼
 ┌────────────────────────────────────────────────────────┐
 │            Zotero 10 MinerU & AI 插件系统              │
 │                                                        │
 │ 1. 适配层 (zotero_adapter.js): 提取 PDF 路径 / 选中文本 │
 │ 2. 解析层 (mineru_client.js):                          │
 │    - 免 Token: /api/v1/agent/parse/file (≤20页/10MB)    │
 │    - 授权 Token: /api/v4/file-urls/batch (≤200页/200MB) │
 │    - 预签名上传 -> 异步轮询 -> 下载结构化 full.md       │
 │ 3. 智能层 (llm_client.js): 汇入上下文 -> 流式科研问答  │
 │ 4. 表现层 (UI & KaTeX): Markdown+数学公式多端渲染     │
 └──────────────────────────┬─────────────────────────────┘
                            ▼
 ┌────────────────────────────────────────────────────────┐
 │                      科研产出沉淀                      │
 │ - 生成含完整公式与版面的 Zotero 子笔记 (Markdown Note) │
 │ - 自动为条目追加 #MinerU 结构化索引标签                │
 │ - 在右侧抽屉流式输出学术洞见、方法提取与中文精读       │
 └────────────────────────────────────────────────────────┘
```
